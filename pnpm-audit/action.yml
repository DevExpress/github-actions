name: pnpm Audit
description: A GitHub Action to run pnpm audit and report vulnerabilities

inputs:
  target:
    type: string
    description: The root path of the repository to audit
    required: true

  artifacts:
    type: string
    description: The path to the artifacts folder where reports will be saved
    required: true

runs:
  using: node16
  main: dist/index.js
